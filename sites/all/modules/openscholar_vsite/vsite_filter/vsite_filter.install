<?php

function vsite_filter_install() {
  $weight = db_result(db_query("SELECT weight FROM {system} WHERE name = 'filter'"));
  db_query("UPDATE {system} SET weight = %d WHERE name = 'vsite_filter'", $weight+1);
}

function vsite_filter_enable() {
  vsite_filter_reset();
}